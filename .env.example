# This project is a static client-side app and does not require environment variables.
# Keep this file for future integrations (analytics, APIs, etc.).

# Example:
# PUBLIC_API_BASE_URL=https://api.example.com

